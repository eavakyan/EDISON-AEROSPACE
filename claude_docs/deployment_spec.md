{
  "content": "\n\n# Deployment Specification \u2014 Edison Aerospace Corporate Website\n\n**Project:** Edison Aerospace Corporate Website (`edison.aero`)\n**Version:** 1.0\n**Last Updated:** 2025-01-XX\n**Author:** DevOps Engineering\n\n---\n\n## Table of Contents\n\n1. [Infrastructure Requirements](#1-infrastructure-requirements)\n2. [Docker Configuration](#2-docker-configuration)\n3. [Nginx Configuration](#3-nginx-configuration)\n4. [Environment Variables](#4-environment-variables)\n5. [SSL/TLS Setup](#5-ssltls-setup)\n6. [Security Hardening Checklist](#6-security-hardening-checklist)\n7. [Monitoring & Logging](#7-monitoring--logging)\n8. [Backup Strategy](#8-backup-strategy)\n9. [CI/CD Pipeline](#9-cicd-pipeline)\n\n---\n\n## 1. Infrastructure Requirements\n\n### VPS Specifications\n\n| Resource | Minimum | Recommended | Notes |\n|----------|---------|-------------|-------|\n| **CPU** | 1 vCPU | 2 vCPU | Static site \u2014 minimal CPU needed |\n| **RAM** | 1 GB | 2 GB | Nginx + Docker overhead |\n| **Storage** | 20 GB SSD | 40 GB SSD | Site assets (video), logs, Docker images |\n| **Bandwidth** | 1 TB/mo | 2 TB/mo | Video backgrounds increase transfer |\n| **OS** | Ubuntu 22.04 LTS | Ubuntu 24.04 LTS | Long-term support, wide ecosystem |\n\n### Networking\n\n| Component | Configuration |\n|-----------|---------------|\n| **Public IP** | 1x static IPv4 (TBD during provisioning) |\n| **Domain** | `edison.aero` (or TBD) |\n| **DNS A Record** | `edison.aero` \u2192 `<VPS_IP>` |\n| **DNS A Record** | `www.edison.aero` \u2192 `<VPS_IP>` |\n| **Ports (Inbound)** | `22/tcp` (SSH), `80/tcp` (HTTP), `443/tcp` (HTTPS) |\n| **Ports (Outbound)** | All (updates, DNS, Certbot ACME) |\n| **Firewall** | UFW with default deny inbound |\n\n### DNS Configuration\n\n```\n; A Records\nedison.aero.        300  IN  A      <VPS_IP>\nwww.edison.aero.    300  IN  A      <VPS_IP>\n\n; CAA Record (optional, restricts CA to Let's Encrypt)\nedison.aero.        300  IN  CAA    0 issue \"letsencrypt.org\"\n```\n\n### VPS Initial Provisioning Script\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\n# -----------------------------------------------\n# VPS Bootstrap Script \u2014 Edison Aerospace\n# Run as root on a fresh Ubuntu 22.04/24.04 LTS\n# -----------------------------------------------\n\nexport DEBIAN_FRONTEND=noninteractive\n\necho \"==> System update\"\napt update && apt upgrade -y\n\necho \"==> Install essentials\"\napt install -y \\\n  curl wget git ufw fail2ban \\\n  apt-transport-https ca-certificates \\\n  gnupg lsb-release software-properties-common \\\n  logrotate unattended-upgrades\n\necho \"==> Create deploy user\"\nuseradd -m -s /bin/bash -G sudo deploy\nmkdir -p /home/deploy/.ssh\ncp /root/.ssh/authorized_keys /home/deploy/.ssh/authorized_keys\nchown -R deploy:deploy /home/deploy/.ssh\nchmod 700 /home/deploy/.ssh\nchmod 600 /home/deploy/.ssh/authorized_keys\n\necho \"==> Harden SSH\"\nsed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config\nsed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config\nsed -i 's/#PubkeyAuthentication yes/PubkeyAuthentication yes/' /etc/ssh/sshd_config\nsystemctl restart sshd\n\necho \"==> Configure UFW\"\nufw default deny incoming\nufw default allow outgoing\nufw allow 22/tcp\nufw allow 80/tcp\nufw allow 443/tcp\nufw --force enable\n\necho \"==> Install Docker\"\ncurl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg\necho \"deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable\" > /etc/apt/sources.list.d/docker.list\napt update\napt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin\nusermod -aG docker deploy\n\necho \"==> Install Certbot\"\napt install -y certbot\n\necho \"==> Create site directory\"\nmkdir -p /var/www/edison-aerospace\nchown deploy:deploy /var/www/edison-aerospace\n\necho \"==> Enable unattended upgrades\"\ndpkg-reconfigure -plow unattended-upgrades\n\necho \"==> Done. Reboot recommended.\"\n```\n\n---\n\n## 2. Docker Configuration\n\n> **Architecture Note:** The project spec states \"Nginx (no Docker)\" for the web server, but the confirmed tech stack includes Docker containerization. This spec uses Docker to containerize the **Astro build process** and the **Nginx static server** together, providing reproducible builds and deployments. A non-Docker fallback is documented in [Section 9](#fallback-manual-rsync-deployment).\n\n### Project Directory Structure\n\n```\nedison-aerospace/\n\u251c\u2500\u2500 src/                          # Astro source files\n\u2502   \u251c\u2500\u2500 components/\n\u2502   \u251c\u2500\u2500 layouts/\n\u2502   \u251c\u2500\u2500 pages/\n\u2502   \u2514\u2500\u2500 styles/\n\u251c\u2500\u2500 public/                       # Static assets (images, video, fonts)\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u251c\u2500\u2500 video/\n\u2502   \u251c\u2500\u2500 sitemap.xml\n\u2502   \u2514\u2500\u2500 robots.txt\n\u251c\u2500\u2500 astro.config.mjs\n\u251c\u2500\u2500 tsconfig.json\n\u251c\u2500\u2500 package.json\n\u251c\u2500\u2500 pnpm-lock.yaml\n\u251c\u2500\u2500 Dockerfile                    # Multi-stage: build + serve\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 docker-compose.prod.yml\n\u251c\u2500\u2500 nginx/\n\u2502   \u251c\u2500\u2500 nginx.conf                # Main Nginx config\n\u2502   \u251c\u2500\u2500 conf.d/\n\u2502   \u2502   \u2514\u2500\u2500 edison-aerospace.conf # Site vhost config\n\u2502   \u2514\u2500\u2500 snippets/\n\u2502       \u251c\u2500\u2500 security-headers.conf\n\u2502       \u251c\u2500\u2500 ssl-params.conf\n\u2502       \u2514\u2500\u2500 cache-static.conf\n\u251c\u2500\u2500 .env.example\n\u251c\u2500\u2500 .env.production\n\u251c\u2500\u2500 .github/\n\u2502   \u2514\u2500\u2500 workflows/\n\u2502       \u2514\u2500\u2500 deploy.yml\n\u251c\u2500\u2500 scripts/\n\u2502   \u251c\u2500\u2500 deploy.sh\n\u2502   \u2514\u2500\u2500 ssl-init.sh\n\u2514\u2500\u2500 .dockerignore\n```\n\n### `.dockerignore`\n\n```dockerignore\nnode_modules\n.git\n.github\n.env*\n!.env.example\ndist\n*.md\nLICENSE\n.vscode\n.idea\nscripts\n```\n\n### `Dockerfile`\n\n```dockerfile\n# ============================================\n# Edison Aerospace \u2014 Multi-Stage Dockerfile\n# Stage 1: Build Astro static site\n# Stage 2: Serve with Nginx\n# ============================================\n\n# --- Stage 1: Build ---\nFROM node:20-alpine AS builder\n\n# Install pnpm\nRUN corepack enable && corepack prepare pnpm@latest --activate\n\nWORKDIR /app\n\n# Copy dependency manifests first (layer caching)\nCOPY package.json pnpm-lock.yaml ./\n\n# Install dependencies\nRUN pnpm install --frozen-lockfile\n\n# Copy source files\nCOPY astro.config.mjs tsconfig.json ./\nCOPY src/ ./src/\nCOPY public/ ./public/\n\n# Build args for build-time variables\nARG FORMSPREE_FORM_ID=FORM_ID\nARG GA4_TAG_ID=GT-WVCKHNB\nARG MAILCHIMP_ACTION_URL=https://placeholder.mailchimp.com\nARG SITE_URL=https://edison.aero\n\n# Set environment variables for Astro build\nENV PUBLIC_FORMSPREE_FORM_ID=${FORMSPREE_FORM_ID}\nENV PUBLIC_GA4_TAG_ID=${GA4_TAG_ID}\nENV PUBLIC_MAILCHIMP_ACTION_URL=${MAILCHIMP_ACTION_URL}\nENV SITE_URL=${SITE_URL}\n\n# Build static site\nRUN pnpm run build\n\n# --- Stage 2: Serve ---\nFROM nginx:1.27-alpine AS production\n\n# Remove default Nginx config and welcome page\nRUN rm -rf /etc/nginx/conf.d/default.conf /usr/share/nginx/html/*\n\n# Copy Nginx configuration\nCOPY nginx/nginx.conf /etc/nginx/nginx.conf\nCOPY nginx/conf.d/ /etc/nginx/conf.d/\nCOPY nginx/snippets/ /etc/nginx/snippets/\n\n# Copy built static files from builder stage\nCOPY --from=builder /app/dist /var/www/edison-aerospace\n\n# Create cache and log directories\nRUN mkdir -p /var/cache/nginx /var/log/nginx && \\\n    chown -R nginx:nginx /var/cache/nginx /var/log/nginx /var/www/edison-aerospace\n\n# Healthcheck\nHEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \\\n  CMD wget --no-verbose --tries=1 --spider http://localhost:80/healthz || exit 1\n\nEXPOSE 80\nEXPOSE 443\n\n# Run as non-root (nginx user is built in)\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n\n### `docker-compose.yml` (Development / Base)\n\n```yaml\nversion: \"3.9\"\n\nservices:\n  web:\n    build:\n      context: .\n      dockerfile: Dockerfile\n      args:\n        FORMSPREE_FORM_ID: ${FORMSPREE_FORM_ID:-FORM_ID}\n        GA4_TAG_ID: ${GA4_TAG_ID:-GT-WVCKHNB}\n        MAILCHIMP_ACTION_URL: ${MAILCHIMP_ACTION_URL:-https://placeholder.mailchimp.com}\n        SITE_URL: ${SITE_URL:-http://localhost}\n    container_name: edison-aerospace-web\n    restart: unless-stopped\n    ports:\n      - \"80:80\"\n    volumes:\n      - ./nginx/conf.d:/etc/nginx/conf.d:ro\n      - ./nginx/snippets:/etc/nginx/snippets:ro\n      - nginx-logs:/var/log/nginx\n    healthcheck:\n      test: [\"CMD\", \"wget\", \"--no-verbose\", \"--tries=1\", \"--spider\", \"http://localhost:80/healthz\"]\n      interval: 30s\n      timeout: 5s\n      retries: 3\n      start_period: 10s\n\nvolumes:\n  nginx-logs:\n    driver: local\n```\n\n### `docker-compose.prod.yml` (Production Override)\n\n```yaml\nversion: \"3.9\"\n\nservices:\n  web:\n    build:\n      args:\n        FORMSPREE_FORM_ID: ${FORMSPREE_FORM_ID}\n        GA4_TAG_ID: ${GA4_TAG_ID}\n        MAILCHIMP_ACTION_URL: ${MAILCHIMP_ACTION_URL}\n        SITE_URL: https://edison.aero\n    container_name: edison-aerospace-web\n    restart: always\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx/conf.d:/etc/nginx/conf.d:ro\n      - ./nginx/snippets:/etc/nginx/snippets:ro\n      - nginx-logs:/var/log/nginx\n      - certbot-webroot:/var/www/certbot:ro\n      - certbot-certs:/etc/letsencrypt:ro\n    depends_on:\n      - certbot\n\n  certbot:\n    image: certbot/certbot:latest\n    container_name: edison-aerospace-certbot\n    volumes:\n      - certbot-webroot:/var/www/certbot\n      - certbot-certs:/etc/letsencrypt\n    # Initial cert issuance \u2014 runs once then exits\n    entrypoint: \"/bin/sh -c 'trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot --quiet; sleep 12h & wait $${!}; done;'\"\n\nvolumes:\n  nginx-logs:\n    driver: local\n  certbot-webroot:\n    driver: local\n  certbot-certs:\n    driver: local\n```\n\n---\n\n## 3. Nginx Configuration\n\n### `nginx/nginx.conf` \u2014 Main Configuration\n\n```nginx\n# ============================================\n# Edison Aerospace \u2014 Main Nginx Configuration\n# ============================================\n\nuser nginx;\nworker_processes auto;\npid /run/nginx.pid;\nerror_log /var/log/nginx/error.log warn;\n\nevents {\n    worker_connections 1024;\n    multi_accept on;\n    use epoll;\n}\n\nhttp {\n    # ---------- Basic Settings ----------\n    include       /etc/nginx/mime.types;\n    default_type  application/octet-stream;\n\n    # Logging format\n    log_format main '$remote_addr - $remote_user [$time_local] '\n                    '\"$request\" $status $body_bytes_sent '\n                    '\"$http_referer\" \"$http_user_agent\" '\n                    'rt=$request_time';\n\n    access_log /var/log/nginx/access.log main;\n\n    sendfile        on;\n    tcp_nopush      on;\n    tcp_nodelay     on;\n    keepalive_timeout 65;\n    types_hash_max_size 2048;\n\n    # Hide Nginx version\n    server_tokens off;\n\n    # ---------- Gzip Compression ----------\n    gzip on;\n    gzip_vary on;\n    gzip_proxied any;\n    gzip_comp_level 6;\n    gzip_min_length 256;\n    gzip_types\n        text/plain\n        text/css\n        text/xml\n        text/javascript\n        application/json\n        application/javascript\n        application/x-javascript\n        application/xml\n        application/xml+rss\n        application/xhtml+xml\n        application/atom+xml\n        image/svg+xml\n        image/x-icon\n        font/woff\n        font/woff2;\n\n    # ---------- Rate Limiting ----------\n    limit_req_zone $binary_remote_addr zone=general:10m rate=10r/s;\n    limit_req_zone $binary_remote_addr zone=static:10m rate=50r/s;\n\n    # ---------- Include Site Configs ----------\n    include /etc/nginx/conf.d/*.conf;\n}\n```\n\n### `nginx/conf.d/edison-aerospace.conf` \u2014 Site Virtual Host\n\n```nginx\n# ============================================\n# Edison Aerospace \u2014 Site Virtual Host\n# ============================================\n\n# --- HTTP \u2192 HTTPS Redirect ---\nserver {\n    listen 80;\n    listen [::]:80;\n    server_name edison.aero www.edison.aero;\n\n    # Certbot ACME challenge\n    location /.well-known/acme-challenge/ {\n        root /var/www/certbot;\n    }\n\n    # Healthcheck endpoint (pre-SSL)\n    location = /healthz {\n        access_log off;\n        return 200 \"OK\\n\";\n        add_header Content-Type text/plain;\n    }\n\n    # Redirect all other HTTP traffic to HTTPS\n    location / {\n        return 301 https://edison.aero$request_uri;\n    }\n}\n\n# --- WWW \u2192 Apex Redirect (HTTPS) ---\nserver {\n    listen 443 ssl http2;\n    listen [::]:443 ssl http2;\n    server_name www.edison.aero;\n\n    include /etc/nginx/snippets/ssl-params.conf;\n\n    ssl_certificate     /etc/letsencrypt/live/edison.aero/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/edison.aero/privkey.pem;\n\n    return 301 https://edison.aero$request_uri;\n}\n\n# --- Main HTTPS Server Block ---\nserver {\n    listen 443 ssl http2;\n    listen [::]:443 ssl http2;\n    server_name edison.aero;\n\n    # SSL Certificates\n    ssl_certificate     /etc/letsencrypt/live/edison.aero/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/edison.aero/privkey.pem;\n\n    # SSL Parameters\n    include /etc/nginx/snippets/ssl-params.conf;\n\n    # Security Headers\n    include /etc/nginx/snippets/security-headers.conf;\n\n    # Document Root\n    root /var/www/edison-aerospace;\n    index index.html;\n\n    # ---------- Error Pages ----------\n    error_page 404 /404.html;\n    error_page 500 502 503 504 /50x.html;\n\n    location = /404.html {\n        root /var/www/edison-aerospace;\n        internal;\n    }\n\n    location = /50x.html {\n        root /var/www/edison-aerospace;\n        internal;\n    }\n\n    # ---------- Healthcheck ----------\n    location = /healthz {\n        access_log off;\n        return 200 \"OK\\n\";\n        add_header Content-Type text/plain;\n    }\n\n    # ---------- Robots & Sitemap ----------\n    location = /robots.txt {\n        access_log off;\n        log_not_found off;\n    }\n\n    location = /sitemap.xml {\n        access_log off;\n    }\n\n    # ---------- Favicon ----------\n    location = /favicon.ico {\n        access_log off;\n        log_not_found off;\n        expires 30d;\n    }\n\n    # ---------- Static Asset Caching ----------\n    include /etc/nginx/snippets/cache-static.conf;\n\n    # ---------- Main Location ----------\n    location / {\n        limit_req zone=general burst=20 nodelay;\n        try_files $uri $uri/ $uri.html =404;\n    }\n\n    # ---------- Deny Hidden Files ----------\n    location ~ /\\. {\n        deny all;\n        access_log off;\n        log_not_found off;\n    }\n\n    # ---------- Deny Access to Sensitive Files ----------\n    location ~* \\.(env|git|gitignore|dockerignore|yml|yaml|toml|lock|md)$ {\n        deny all;\n        return 404;\n    }\n}\n```\n\n### `nginx/snippets/ssl-params.conf`\n\n```nginx\n# ============================================\n# SSL/TLS Parameters\n# Mozilla Modern Configuration\n# https://ssl-config.mozilla.org/\n# ============================================\n\nssl_protocols TLSv1.2 TLSv1.3;\nssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;\nssl_prefer_server_ciphers off;\n\n# SSL session\nssl_session_timeout 1d;\nssl_session_cache shared:SSL:10m;\nssl_session_tickets off;\n\n# OCSP Stapling\nssl_stapling on;\nssl_stapling_verify on;\nresolver 1.1.1.1 8.8.8.8 valid=300s;\nresolver_timeout 5s;\n\n# Diffie-Hellman parameter (generate with: openssl dhparam -out /etc/nginx/dhparam.pem 4096)\n# Uncomment after generating:\n# ssl_dhparam /etc/nginx/dhparam.pem;\n```\n\n### `nginx/snippets/security-headers.conf`\n\n```nginx\n# ============================================\n# Security Headers\n# ============================================\n\n# Prevent clickjacking\nadd_header X-Frame-Options \"SAMEORIGIN\" always;\n\n# Prevent MIME-type sniffing\nadd_header X-Content-Type-Options \"nosniff\" always;\n\n# XSS Protection (legacy browsers)\nadd_header X-XSS-Protection \"1; mode=block\" always;\n\n# HSTS \u2014 enforce HTTPS for 1 year, include subdomains, preload eligible\nadd_header Strict-Transport-Security \"max-age=31536000; includeSubDomains; preload\" always;\n\n# Referrer Policy\nadd_header Referrer-Policy \"strict-origin-when-cross-origin\" always;\n\n# Permissions Policy \u2014 restrict browser features\nadd_header Permissions-Policy \"camera=(), microphone=(), geolocation=(), payment=(), usb=()\" always;\n\n# Content Security Policy\n# NOTE: Carefully tuned for Edison Aerospace's third-party integrations\nadd_header Content-Security-Policy \"\n    default-src 'self';\n    script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com https://formspree.io;\n    style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;\n    font-src 'self' https://fonts.gstatic.com;\n    img-src 'self' data: https://www.google-analytics.com https://*.mailchimp.com;\n    media-src 'self';\n    frame-src https://www.youtube-nocookie.com https://formspree.io https://*.list-manage.com;\n    connect-src 'self' https://www.google-analytics.com https://formspree.io https://*.mailchimp.com;\n    object-src 'none';\n    base-uri 'self';\n    form-action 'self' https://formspree.io https://*.list-manage.com;\n    frame-ancestors 'self';\n    upgrade-insecure-requests;\n\" always;\n```\n\n### `nginx/snippets/cache-static.conf`\n\n```nginx\n# ============================================\n# Static Asset Cache Headers\n# 1-year cache for fingerprinted assets\n# ============================================\n\n# CSS and JavaScript (Astro fingerprints filenames with content hashes)\nlocation ~* \\.(css|js)$ {\n    expires 1y;\n    add_header Cache-Control \"public, immutable\";\n    access_log off;\n    limit_req zone=static burst=50 nodelay;\n    include /etc/nginx/snippets/security-headers.conf;\n}\n\n# Images\nlocation ~* \\.(jpg|jpeg|png|gif|ico|svg|webp|avif)$ {\n    expires 1y;\n    add_header Cache-Control \"public, immutable\";\n    access_log off;\n    limit_req zone=static burst=50 nodelay;\n    include /etc/nginx/snippets/security-headers.conf;\n}\n\n# Video files\nlocation ~* \\.(mp4|webm|ogg)$ {\n    expires 30d;\n    add_header Cache-Control \"public\";\n    access_log off;\n    include /etc/nginx/snippets/security-headers.conf;\n}\n\n# Fonts\nlocation ~* \\.(woff|woff2|ttf|otf|eot)$ {\n    expires 1y;\n    add_header Cache-Control \"public, immutable\";\n    add_header Access-Control-Allow-Origin \"*\";\n    access_log off;\n    include /etc/nginx/snippets/security-headers.conf;\n}\n\n# HTML \u2014 short cache, always revalidate\nlocation ~* \\.html$ {\n    expires 1h;\n    add_header Cache-Control \"public, must-revalidate\";\n    include /etc/nginx/snippets/security-headers.conf;\n}\n\n# XML (sitemap) and TXT (robots)\nlocation ~* \\.(xml|txt)$ {\n    expires 1d;\n    add_header Cache-Control \"public\";\n    access_log off;\n}\n```\n\n---\n\n## 4. Environment Variables\n\n### `.env.example`\n\n```bash\n# ============================================\n# Edison Aerospace \u2014 Environment Variables\n# Copy to .env.production and fill in values\n# ============================================\n\n# ----- Domain & Site -----\n# The public URL of the site (no trailing slash)\nSITE_URL=https://edison.aero\n\n# Domain name (used by Certbot and Nginx)\nDOMAIN=edison.aero\n\n# ----- Formspree -----\n# Contact form endpoint ID from Formspree dashboard\n# Create form at https://formspree.io and paste the form ID\nFORMSPREE_FORM_ID=FORM_ID\n\n# ----- Google Analytics GA4 -----\n# Google Analytics 4 Measurement ID / Google Tag ID\nGA4_TAG_ID=GT-WVCKHNB\n\n# ----- Mailchimp -----\n# Mailchimp embedded form action URL for newsletter signup\n# Get from Mailchimp \u2192 Audience \u2192 Signup forms \u2192 Embedded forms\nMAILCHIMP_ACTION_URL=https://placeholder.us1.list-manage.com/subscribe/post?u=XXXXXXX&id=XXXXXXX\n\n# ----- Certbot / SSL -----\n# Email address for Let's Encrypt certificate notifications\nCERTBOT_EMAIL=admin@edison.aero\n\n# ----- Deployment (CI/CD) -----\n# VPS IP address for SSH/rsync deployment\nVPS_IP=0.0.0.0\n\n# SSH user on the VPS\nDEPLOY_USER=deploy\n\n# SSH port (default 22)\nDEPLOY_SSH_PORT=22\n\n# Path on VPS where site files are deployed\nDEPLOY_PATH=/var/www/edison-aerospace\n\n# ----- Docker -----\n# Docker Compose project name\nCOMPOSE_PROJECT_NAME=edison-aerospace\n```\n\n### Variable Reference Table\n\n| Variable | Required | Default | Description |\n|----------|----------|---------|-------------|\n| `SITE_URL` | Yes | `https://edison.aero` | Canonical site URL for Astro build, SEO meta tags, sitemap |\n| `DOMAIN` | Yes | `edison.aero` | Domain for Nginx server_name and Certbot |\n| `FORMSPREE_FORM_ID` | Yes | `FORM_ID` | Formspree endpoint ID \u2014 replace before production |\n| `GA4_TAG_ID` | Yes | `GT-WVCKHNB` | Google Analytics 4 tag for tracking snippet |\n| `MAILCHIMP_ACTION_URL` | Yes | (placeholder) | Mailchimp embedded form POST URL |\n| `CERTBOT_EMAIL` | Yes | `admin@edison.aero` | Let's Encrypt expiry notifications email |\n| `VPS_IP` | Yes (CI/CD) | `0.0.0.0` | Target VPS for deployment |\n| `DEPLOY_USER` | Yes (CI/CD) | `deploy` | SSH user for rsync deployment |\n| `DEPLOY_SSH_PORT` | No | `22` | SSH port on VPS |\n| `DEPLOY_PATH` | Yes (CI/CD) | `/var/www/edison-aerospace` | Server document root path |\n| `COMPOSE_PROJECT_NAME` | No | `edison-aerospace` | Docker Compose project namespace |\n\n> **Security Note:** All integrations (Formspree, GA4, Mailchimp, YouTube, Google Fonts) are **client-side only**. No server-side API keys or secrets are required. The `FORMSPREE_FORM_ID` and `MAILCHIMP_ACTION_URL` are embedded in public HTML \u2014 they are not secrets but should be replaced from placeholders before production go-live.\n\n---\n\n## 5. SSL/TLS Setup\n\n### Initial Certificate Provisioning Script\n\n#### `scripts/ssl-init.sh`\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\n# ============================================\n# Edison Aerospace \u2014 SSL Initial Setup\n# Run ONCE on VPS to obtain initial Let's Encrypt cert\n# ============================================\n\n# Load environment\nsource .env.production\n\nDOMAIN=\"${DOMAIN:-edison.aero}\"\nCERTBOT_EMAIL=\"${CERTBOT_EMAIL:-admin@edison.aero}\"\n\necho \"==> Step 1: Ensure HTTP-only Nginx is running for ACME challenge\"\n\n# Create a temporary Nginx config for cert issuance (HTTP only)\ncat > /tmp/nginx-certbot-temp.conf <<'NGINX_TEMP'\nevents { worker_connections 1024; }\nhttp {\n    server {\n        listen 80;\n        server_name edison.aero www.edison.aero;\n\n        location /.well-known/acme-challenge/ {\n            root /var/www/certbot;\n        }\n\n        location / {\n            return 200 \"Awaiting SSL setup\";\n            add_header Content-Type text/plain;\n        }\n    }\n}\nNGINX_TEMP\n\n# Stop existing containers\ndocker compose -f docker-compose.yml -f docker-compose.prod.yml down 2>/dev/null || true\n\n# Create certbot webroot\nmkdir -p /var/www/certbot\n\n# Start temporary Nginx for ACME challenge\ndocker run -d --name nginx-certbot-temp \\\n  -p 80:80 \\\n  -v /tmp/nginx-certbot-temp.conf:/etc/nginx/nginx.conf:ro \\\n  -v /var/www/certbot:/var/www/certbot \\\n  nginx:1.27-alpine\n\necho \"==> Step 2: Request certificate from Let's Encrypt\"\n\ndocker run --rm \\\n  -v /etc/letsencrypt:/etc/letsencrypt \\\n  -v /var/www/certbot:/var/www/certbot \\\n  certbot/certbot certonly \\\n    --webroot \\\n    --webroot-path=/var/www/certbot \\\n    --email \"${CERTBOT_EMAIL}\" \\\n    --agree-tos \\\n    --no-eff-email \\\n    --force-renewal \\\n    -d \"${DOMAIN}\" \\\n    -d \"www.${DOMAIN}\"\n\necho \"==> Step 3: Stop temporary Nginx\"\ndocker stop nginx-certbot-temp && docker rm nginx-certbot-temp\nrm /tmp/nginx-certbot-temp.conf\n\necho \"==> Step 4: Generate Diffie-Hellman parameters (this takes a few minutes)\"\nif [ ! -f /etc/nginx/dhparam.pem ]; then\n    openssl dhparam -out /etc/nginx/dhparam.pem 4096\n    echo \"    DH params generated at /etc/nginx/dhparam.pem\"\nelse\n    echo \"    DH params already exist, skipping\"\nfi\n\necho \"==> Step 5: Start full production stack\"\ndocker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build\n\necho \"\"\necho \"==> SSL setup complete!\"\necho \"    Certificate: /etc/letsencrypt/live/${DOMAIN}/fullchain.pem\"\necho \"    Private Key: /etc/letsencrypt/live/${DOMAIN}/privkey.pem\"\necho \"\"\necho \"    Verify: curl -I https://${DOMAIN}\"\n```\n\n### Automatic Certificate Renewal\n\nCertbot runs inside the `certbot` Docker service (see `docker-compose.prod.yml`) with a renewal loop every 12 hours. Additionally, add a **host-level cron** as a safety net:\n\n```bash\n# Add to deploy user's crontab: crontab -e\n# Renew certs twice daily and reload Nginx if renewed\n0 3,15 * * * docker compose -f /home/deploy/edison-aerospace/docker-compose.yml -f /home/deploy/edison-aerospace/docker-compose.prod.yml exec certbot certbot renew --quiet && docker compose -f /home/deploy/edison-aerospace/docker-compose.yml -f /home/deploy/edison-aerospace/docker-compose.prod.yml exec web nginx -s reload 2>/dev/null\n```\n\n### SSL Verification Commands\n\n```bash\n# Test certificate\ncurl -vI https://edison.aero 2>&1 | grep -E \"subject:|issuer:|expire\"\n\n# SSL Labs test (manual)\n# Visit: https://www.ssllabs.com/ssltest/analyze.html?d=edison.aero\n\n# Check certificate expiry\necho | openssl s_client -servername edison.aero -connect edison.aero:",
  "version": 1,
  "last_edited": "2026-03-09T03:32:12.202000"
}