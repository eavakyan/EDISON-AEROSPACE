{
  "content": "\n\n# Phased Implementation Plan: Edison Aerospace Corporate Website\n\n## Overview\n\nThis plan breaks the Edison Aerospace static corporate website into 8 sequential phases, building from infrastructure through content pages to deployment. Each phase produces independently verifiable output. The tech stack is Astro (TypeScript) with pnpm, Vitest for testing, and Docker + Nginx for deployment.\n\n---\n\n## Phase 0: Infrastructure & Scaffold\n\n**Goal:** Establish the project skeleton, tooling, dev environment, Docker setup, and CI/CD pipeline so all subsequent phases have a solid foundation.\n\n### Deliverables\n- Initialized Astro project with TypeScript, pnpm, and project structure\n- Docker + Nginx development and production configurations\n- GitHub Actions CI/CD pipeline (lint, build, deploy via rsync)\n- Base layout component with HTML5 boilerplate, meta tag slots, and Schema.org JSON-LD slots\n- Google Fonts loaded (Montserrat, Josefin Sans, Open Sans, Roboto Condensed, Karla, Oswald)\n- CSS design tokens / global stylesheet with color palette, typography scale, spacing system\n- Placeholder `index.astro` page confirming the full pipeline works end-to-end\n- Vitest configured with a smoke test\n\n### Tasks (12)\n1. `pnpm create astro@latest` with TypeScript strict mode; configure `astro.config.mjs` for static output\n2. Define folder structure: `src/layouts/`, `src/components/`, `src/pages/`, `src/styles/`, `src/assets/`, `src/data/`, `public/`\n3. Create `global.css` with CSS custom properties: colors (`--color-primary: #052c54`, `--color-accent: #f83aff`, `--color-secondary: #5325ea`), font stacks, spacing scale, reset/normalize\n4. Add Google Fonts via `<link>` preconnect + stylesheet in base layout `<head>`\n5. Create `BaseLayout.astro` with `<html>`, `<head>` (charset, viewport, title slot, meta description slot, OG/Twitter card slots, canonical URL slot, Schema.org JSON-LD slot, favicon), `<body>` with `<slot />` for page content\n6. Create `src/data/` directory with placeholder TypeScript data files for navigation, social links, site metadata\n7. Create placeholder `src/pages/index.astro` using `BaseLayout` with \"Hello Edison Aerospace\" content\n8. Write `Dockerfile` (multi-stage: Node build \u2192 Nginx serve) and `docker-compose.yml` for local dev\n9. Write `nginx.conf` with gzip, static asset caching (1yr), security headers (X-Frame-Options, X-Content-Type-Options, HSTS, CSP), and HTML no-cache\n10. Create `.github/workflows/deploy.yml`: checkout \u2192 pnpm install \u2192 build \u2192 rsync to VPS over SSH (with secrets placeholders)\n11. Configure Vitest in `vitest.config.ts`; write smoke test that imports `BaseLayout` and verifies it renders without error\n12. Add `.editorconfig`, `.prettierrc`, `tsconfig.json` strict settings, `.gitignore`\n\n### Acceptance Criteria\n- [ ] `pnpm dev` serves site locally at `localhost:4321` with styled placeholder page\n- [ ] `pnpm build` outputs static files to `dist/` with zero errors/warnings\n- [ ] `docker compose up` serves the built site via Nginx on `localhost:8080` with correct response headers (gzip, security headers)\n- [ ] `pnpm test` passes smoke test\n- [ ] GitHub Actions workflow file parses without errors (validated via `act` or dry-run)\n- [ ] All six Google Fonts families load and render correctly\n- [ ] CSS custom properties for all three brand colors are applied and visible in dev tools\n\n### Dependencies\n- None (this is Phase 0)\n\n---\n\n## Phase 1: Global Components \u2014 Header, Footer, Navigation\n\n**Goal:** Build the persistent site chrome (header and footer) that appears on every page, including responsive hamburger menu, dropdown navigation, and footer with social/newsletter.\n\n### Deliverables\n- `Header.astro` component: sticky header with logo, full desktop nav, Agriculture dropdown with 5 sub-pages, mobile hamburger menu\n- `MobileMenu.astro` component: right-side sliding overlay panel with full nav tree\n- `Footer.astro` component: social links (Facebook, Instagram, YouTube), secondary navigation, newsletter signup (Mailchimp placeholder), copyright\n- `Navigation` TypeScript data file with full nav tree\n- `SocialLinks` TypeScript data file\n- `BaseLayout` updated to include Header and Footer\n- Cookie consent banner component\n- Search toggle button (UI only \u2014 opens/closes search overlay placeholder)\n\n### Tasks (11)\n1. Create `src/data/navigation.ts` \u2014 typed array of `NavigationItem` objects with parent/children, display order, CTA flags, matching all pages from spec\n2. Create `src/data/socialLinks.ts` \u2014 typed array for Facebook, Instagram, YouTube with icon identifiers\n3. Build `Header.astro`: fixed/sticky positioning, logo image (left), desktop nav (center/right) with flex layout, active page highlighting via `Astro.url.pathname`\n4. Build `NavDropdown.astro`: Agriculture dropdown component triggered on hover (desktop) showing Heavy 1, Ground Control Station, Cost of Ownership, Safety, Pilot Training sub-links\n5. Build hamburger icon button (CSS-only animated three-line \u2192 X transition)\n6. Build `MobileMenu.astro`: right-side sliding overlay panel (100vh, z-indexed above content), full nav tree with expandable Agriculture section, close button \u2014 uses Astro `client:load` island for toggle JS\n7. Build `Footer.astro`: three-column responsive layout \u2014 col 1: logo + company tagline, col 2: secondary nav links, col 3: social media icon links + newsletter signup form\n8. Build `NewsletterSignup.astro`: email input + \"Stay Updated\" label + Subscribe button, form action pointing to Mailchimp placeholder URL\n9. Build `CookieConsent.astro`: bottom bar with message, Accept/Decline buttons \u2014 uses `client:load` island; sets `localStorage` flag; conditionally injects GA4 `gtag.js` on accept\n10. Build `SearchToggle.astro`: magnifying glass icon button in header that toggles a search overlay placeholder (no search functionality \u2014 just UI skeleton)\n11. Write Vitest tests: navigation data structure validation (all pages have URLs, no orphan children), component import tests\n\n### Acceptance Criteria\n- [ ] Header is sticky on scroll, visually matches dark blue brand aesthetic\n- [ ] Desktop: all nav items visible, Agriculture shows dropdown on hover with 5 sub-page links\n- [ ] Mobile (< 768px): hamburger icon visible, clicking opens right-side overlay with full nav, clicking X or outside closes it\n- [ ] Menu underline hover animations work on desktop nav items\n- [ ] Footer renders social icons linking to placeholder URLs, newsletter form with email input, secondary nav links, copyright line\n- [ ] Cookie consent bar appears on first visit, disappears after Accept/Decline, preference persists in localStorage\n- [ ] GA4 script tag only loads after cookie consent acceptance\n- [ ] All components render correctly at 768px and 1140px breakpoints\n- [ ] Keyboard navigation works through all menu items (Tab, Enter, Escape to close)\n\n### Dependencies\n- Phase 0 (BaseLayout, CSS tokens, project structure)\n\n---\n\n## Phase 2: Home Page\n\n**Goal:** Build the complete homepage \u2014 the most visually complex page \u2014 with video hero, value proposition cards, company description, Heavy 1 showcase, and embedded media.\n\n### Deliverables\n- `src/pages/index.astro` \u2014 fully built homepage\n- `VideoHero.astro` component: HTML5 video background with overlay text, CTA button, mobile static image fallback\n- `ValueCards.astro` component: three-column Safer/Cheaper/More Effective cards with icons and hover effects\n- `CompanyDescription.astro` component: text + image section\n- `ProductShowcase.astro` component: Heavy 1 featured section with key specs and image\n- `YouTubeEmbed.astro` component: privacy-enhanced lazy-loaded YouTube iframe\n- Parallax scroll utility (reusable CSS class + minimal JS)\n- All homepage SEO: meta tags, OG/Twitter cards, Schema.org Organization + WebPage JSON-LD\n\n### Tasks (12)\n1. Create `VideoHero.astro`: `<video autoplay muted loop playsinline>` with MP4/WebM sources, dark overlay gradient, tagline text (h1), CTA button \u2014 accepts props for video src, fallback image, tagline, CTA text/link\n2. Implement mobile fallback: CSS `@media` to hide `<video>` below 768px and show background-image instead\n3. Build `ValueCards.astro`: three-column CSS Grid/Flexbox layout, each card with icon (SVG or font icon), heading, description text \u2014 cards have `transform: translateY()` hover effect and color transition on icon box (magenta/purple accents)\n4. Build `CompanyDescription.astro`: two-column layout (text left, image right) with company overview content, responsive stack on mobile\n5. Build `ProductShowcase.astro`: Heavy 1 featured section with product image, key specs (200-gal payload, 40ft wingspan, 3000lb MTOW), \"Learn More\" CTA linking to Agriculture/Heavy-1 page\n6. Build `YouTubeEmbed.astro`: privacy-enhanced iframe (`youtube-nocookie.com`), lazy loading via `loading=\"lazy\"` + Intersection Observer facade (thumbnail click-to-load pattern for performance), accepts `videoId` prop\n7. Create `src/styles/parallax.css` \u2014 reusable parallax classes using `background-attachment: fixed` with `transform` fallback, and small JS utility for smooth parallax on scroll\n8. Compose `index.astro`: import and arrange all sections in order \u2014 VideoHero \u2192 ValueCards \u2192 CompanyDescription \u2192 ProductShowcase \u2192 YouTubeEmbed \u2014 within BaseLayout\n9. Add Schema.org JSON-LD to page: `Organization` (Edison Aerospace LLC, electric aviation, social links) + `WebPage` type\n10. Add complete `<head>` metadata: title, meta description, OG image, OG title/description/url, Twitter card tags, canonical URL\n11. Create placeholder assets: add representative placeholder images/video to `public/assets/` (or `src/assets/` for Astro optimization) \u2014 hero video placeholder, product image, value card icons\n12. Write Vitest test: validate homepage renders all expected sections, meta tags present, Schema.org JSON-LD is valid JSON\n\n### Acceptance Criteria\n- [ ] Homepage loads with autoplay muted video background hero on desktop, static image on mobile\n- [ ] Hero overlay text and CTA button are readable against video background with proper contrast\n- [ ] Three value proposition cards display in row on desktop, stack on mobile, with smooth hover animations\n- [ ] Company description section renders two-column on desktop, stacks on mobile\n- [ ] Heavy 1 showcase displays specs and links to product page\n- [ ] YouTube embed uses facade pattern (thumbnail first, loads iframe on click)\n- [ ] Page scores 90+ on Lighthouse Performance (with placeholder assets)\n- [ ] All Schema.org JSON-LD validates at schema.org validator\n- [ ] OG tags render correct preview in social media debugger tools\n\n### Dependencies\n- Phase 0 (BaseLayout, CSS tokens)\n- Phase 1 (Header, Footer \u2014 page needs chrome)\n\n---\n\n## Phase 3: Agriculture Section (5 Sub-Pages)\n\n**Goal:** Build all five Agriculture sub-pages: Heavy 1, Ground Control Station, Cost of Ownership, Safety, and Pilot Training.\n\n### Deliverables\n- `src/pages/agriculture/heavy-1.astro` \u2014 Heavy 1 product detail page\n- `src/pages/agriculture/ground-control-station.astro` \u2014 GCS product page\n- `src/pages/agriculture/cost-of-ownership.astro` \u2014 Cost comparison page with interactive table\n- `src/pages/agriculture/safety.astro` \u2014 Safety features page\n- `src/pages/agriculture/pilot-training.astro` \u2014 Training program page\n- `ParallaxHero.astro` component (reusable for all inner pages)\n- `SpecsTable.astro` component for product specifications\n- `CostCalculator.astro` interactive island component (client-side JS)\n- `BarChart.astro` CSS/JS bar chart visualization\n- `Breadcrumbs.astro` component with Schema.org BreadcrumbList\n- Product data files in `src/data/`\n- Cost comparison data file in `src/data/`\n\n### Tasks (16)\n1. Create `src/data/products.ts` \u2014 typed Product and ProductSpec data for Heavy 1 (200-gal payload, 40ft wingspan, 3000lb MTOW, electric, etc.) and Ground Control Station\n2. Create `src/data/costComparison.ts` \u2014 typed CostComparison data with Heavy 1 vs traditional manned aircraft costs across small/medium/large acreage scenarios\n3. Build `ParallaxHero.astro`: reusable hero with parallax background image, overlay gradient, page title, optional tagline \u2014 props for image src, title, subtitle\n4. Build `Breadcrumbs.astro`: semantic `<nav aria-label=\"Breadcrumb\">` with Schema.org `BreadcrumbList` JSON-LD, auto-generated from page path\n5. Build `SpecsTable.astro`: responsive specs grid/table accepting product spec array, with label/value/unit display, styled with brand colors\n6. Build `agriculture/heavy-1.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 product description \u2192 SpecsTable \u2192 image gallery placeholder \u2192 CTA button to Contact\n7. Build `agriculture/ground-control-station.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 description \u2192 features list (styled checkmark/icon list) \u2192 images \u2192 CTA\n8. Build `CostCalculator.astro` as Astro interactive island (`client:visible`): three toggle buttons (500 acres / 2000 acres / 5000+ acres), displays comparison table showing Heavy 1 vs traditional costs per category, updates on toggle\n9. Build `BarChart.astro`: pure CSS/JS bar chart visualization showing total annual cost comparison between Heavy 1 and traditional, animates on scroll into view, updates with scenario toggle\n10. Build `agriculture/cost-of-ownership.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 intro text \u2192 CostCalculator \u2192 BarChart \u2192 CTA\n11. Build `agriculture/safety.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 safety features content sections \u2192 statistics callout boxes (large numbers with labels) \u2192 CTA\n12. Build `agriculture/pilot-training.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 training program details \u2192 requirements list \u2192 CTA\n13. Create reusable `CTASection.astro` component: full-width colored band with heading + CTA button linking to Contact page, used on all agriculture pages\n14. Add SEO metadata to all 5 pages: unique titles, meta descriptions, OG tags, canonical URLs, Schema.org (Product type for Heavy 1 and GCS, WebPage for others)\n15. Add placeholder images for all agriculture pages to `src/assets/agriculture/`\n16. Write Vitest tests: cost comparison data validation (all fields present, numbers parseable), product data validation, CostCalculator toggle logic unit test\n\n### Acceptance Criteria\n- [ ] All 5 agriculture pages accessible via nav dropdown and direct URL\n- [ ] Parallax hero effect works on scroll on all pages (desktop), degrades gracefully on mobile\n- [ ] Breadcrumbs show correct hierarchy (Home > Agriculture > [Page]) with valid Schema.org markup\n- [ ] Heavy 1 page displays all specs in formatted table/grid\n- [ ] Cost calculator toggles between 3 acreage scenarios, table and bar chart update correctly\n- [ ] Bar chart animates on first scroll into view\n- [ ] Safety page displays statistics in visually prominent callout boxes\n- [ ] All CTAs link to Contact page\n- [ ] All pages responsive at 768px and 1140px breakpoints\n- [ ] Schema.org Product JSON-LD validates for Heavy 1 and GCS pages\n\n### Dependencies\n- Phase 0 (BaseLayout, CSS tokens)\n- Phase 1 (Header with Agriculture dropdown, Footer)\n- Phase 2 (YouTubeEmbed if any product pages embed video; ParallaxHero concept)\n\n---\n\n## Phase 4: Defense, Press, News, Podcast Pages\n\n**Goal:** Build the remaining four content sections, completing all site pages except Contact.\n\n### Deliverables\n- `src/pages/defense.astro` \u2014 Defense sector page\n- `src/pages/press.astro` \u2014 Press coverage grid\n- `src/pages/news.astro` \u2014 News listing page\n- `src/pages/news/[slug].astro` \u2014 Individual news post pages (if needed, or inline)\n- `src/pages/podcast.astro` \u2014 Podcast episode listing\n- `ArticleCard.astro` component (reusable for press and news)\n- `PodcastEpisode.astro` component\n- Data files: `src/data/pressArticles.ts`, `src/data/newsPosts.ts`, `src/data/podcastEpisodes.ts`, `src/data/defenseProducts.ts`\n\n### Tasks (13)\n1. Create `src/data/defenseProducts.ts` \u2014 typed data for UNCV and aerial defense vehicles (name, description, capabilities, images)\n2. Create `src/data/pressArticles.ts` \u2014 typed PressArticle array with title, source, date, URL, thumbnail, excerpt, display order (5-8 placeholder entries)\n3. Create `src/data/newsPosts.ts` \u2014 typed NewsPost array with title, date, slug, content, image, excerpt (4-6 placeholder entries)\n4. Create `src/data/podcastEpisodes.ts` \u2014 typed PodcastEpisode array with title, date, description, embed URL, external URL, thumbnail (3-5 placeholder entries)\n5. Build `defense.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 UNCV section (description, capabilities, image) \u2192 aerial defense section \u2192 defense capabilities summary \u2192 CTA to Contact\n6. Build `ArticleCard.astro`: card component with thumbnail image, title, source/date metadata, excerpt \u2014 used for both press and news grids\n7. Build `press.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 responsive CSS Grid of ArticleCards (3 columns desktop, 2 tablet, 1 mobile) \u2014 cards link to external article URLs (open in new tab with `rel=\"noopener\"`)\n8. Build `news.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 chronological list/grid of news ArticleCards \u2014 cards link to individual news post pages or anchor sections\n9. Build individual news post template: `src/pages/news/[slug].astro` using Astro dynamic routes with `getStaticPaths()` from news data, rendering full post content with featured image, date, and back-to-news link\n10. Build `PodcastEpisode.astro`: card/row component with episode thumbnail, title, date, description, embedded player (iframe or audio element) or external platform link button\n11. Build `podcast.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 list of PodcastEpisode components\n12. Add SEO metadata to all pages: unique titles, descriptions, OG tags, canonical URLs, Schema.org WebPage JSON-LD\n13. Write Vitest tests: data file validation (dates parseable, URLs valid format, required fields present), dynamic route generation test for news slugs\n\n### Acceptance Criteria\n- [ ] Defense page showcases UNCV and aerial defense with descriptions, images, and CTA\n- [ ] Press page renders grid of article cards with thumbnails, external links open in new tab\n- [ ] News page lists posts chronologically; clicking a post opens its full content page\n- [ ] Individual news post pages render with correct content, back navigation, and proper URL slugs\n- [ ] Podcast page lists episodes with embedded players or external links\n- [ ] All pages use ParallaxHero and Breadcrumbs consistently\n- [ ] Article card grid is responsive across all three breakpoints\n- [ ] All navigation links from header correctly reach these pages\n\n### Dependencies\n- Phase 0 (BaseLayout)\n- Phase 1 (Header, Footer)\n- Phase 3 (ParallaxHero, Breadcrumbs, CTASection \u2014 reused)\n\n---\n\n## Phase 5: Contact Page, Forms & Integrations\n\n**Goal:** Build the Contact page with form, Thank You page, and wire up all third-party integrations (Formspree, GA4, Mailchimp, cookie consent logic).\n\n### Deliverables\n- `src/pages/contact.astro` \u2014 Contact page with form\n- `src/pages/thank-you.astro` \u2014 Form submission confirmation page\n- `ContactForm.astro` interactive island component with client-side validation\n- GA4 integration wired through cookie consent\n- Mailchimp newsletter form wired in footer\n- Formspree form action configured (placeholder endpoint)\n\n### Tasks (10)\n1. Build `ContactForm.astro` (`client:load` island): form with fields \u2014 name (text, required), email (email, required), phone (tel, optional), inquiry type (select: Agriculture/Defense, required), message (textarea, required), submit button\n2. Implement client-side form validation: required field checks, email format regex, phone format hint, visual error states (red border, error message below field), prevent submission until valid\n3. Wire form `action` to Formspree placeholder URL (`https://formspree.io/f/FORM_ID`), method POST, with `_next` redirect to `/thank-you`\n4. Build `contact.astro`: ParallaxHero \u2192 Breadcrumbs \u2192 two-column layout (ContactForm left, contact information right \u2014 company name, email, phone, address placeholders) \u2192 map placeholder or image\n5. Build `thank-you.astro`: simple centered confirmation message (\"Thank you for your inquiry\u2026\"), link back to homepage, no-index meta tag\n6. Wire GA4 integration: create `src/components/Analytics.astro` that conditionally renders `gtag.js` script with tag `GT-WVCKHNB` only when cookie consent localStorage flag is \"accepted\"\n7. Wire Mailchimp newsletter: update `NewsletterSignup.astro` form action to Mailchimp placeholder URL, ensure email field name matches Mailchimp expected `MERGE0`/`EMAIL` field\n8. Update `CookieConsent.astro`: Accept button sets localStorage and dynamically injects GA4 script; Decline sets flag and does not load GA4; returning visitors skip banner based on stored preference\n9. Add SEO metadata to Contact and Thank You pages\n10. Write Vitest tests: form validation logic unit tests (valid/invalid email, required fields, phone format), cookie consent state machine logic\n\n### Acceptance Criteria\n- [ ] Contact form renders with all fields, proper labels, and placeholder text\n- [ ] Client-side validation prevents submission with missing required fields or invalid email\n- [ ] Validation errors display inline below respective fields with accessible ARIA attributes\n- [ ] Form submits via POST to Formspree placeholder URL and redirects to Thank You page\n- [ ] Thank You page displays confirmation message and homepage link\n- [ ] Inquiry type dropdown offers Agriculture and Defense options\n- [ ] GA4 script loads only after cookie consent acceptance, not before\n- [ ] Newsletter signup form in footer submits to Mailchimp placeholder\n- [ ] Contact page is responsive: two-column on desktop, stacked on mobile\n- [ ] All form fields have proper `<label>` elements, ARIA attributes, and keyboard accessibility\n\n### Dependencies\n- Phase 0 (BaseLayout)\n- Phase 1 (Header, Footer with NewsletterSignup, CookieConsent)\n\n---\n\n## Phase 6: Image Lightbox, Animations, Polish & Accessibility\n\n**Goal:** Add visual polish features (lightbox galleries, CSS animations, smooth scroll), ensure WCAG 2.1 AA compliance, and refine the pixel-perfect aesthetic across all pages.\n\n### Deliverables\n- `Lightbox.astro` interactive island component for image galleries\n- Image galleries added to Heavy 1, GCS, Defense, and Press pages\n- CSS animations refined: card hover effects, menu underlines, icon transitions, scroll-triggered animations\n- Smooth scroll behavior on anchor links and page transitions\n- WCAG 2.1 AA audit pass: alt tags, ARIA labels, keyboard nav, color contrast, focus indicators\n- Responsive QA fixes across all pages\n\n### Tasks (12)\n1. Build `Lightbox.astro` (`client:visible` island): click-to-expand overlay with full-size image, close button (X + Escape key + click outside), previous/next navigation for galleries, transition animations\n2. Add image gallery data to Heavy 1, GCS, and Defense product data files; render gallery grid on those pages with Lightbox integration\n3. Add lightbox to Press page article thumbnails (click to expand image before visiting external link)\n4. Refine CSS hover animations: card `transform: scale()` + `box-shadow` transitions, nav menu underline slide animation (`::after` pseudo-element width transition), icon box background-color transitions (to magenta/purple)\n5. Implement scroll-triggered animations: fade-in-up for content sections using Intersection Observer, staggered card reveals, counter animation for safety statistics\n6. Add `scroll-behavior: smooth` to HTML and implement smooth scroll for any in-page anchor links; ensure `prefers-reduced-motion` media query disables animations\n7. WCAG audit \u2014 images: add descriptive `alt` text to every `<img>`, decorative images get `alt=\"\"`\n8. WCAG audit \u2014 ARIA & semantics: add `aria-label` to nav landmarks, `aria-expanded` to hamburger/dropdown, `role` attributes where needed, ensure heading hierarchy (h1\u2192h2\u2192h3) is correct on all pages\n9. WCAG audit \u2014 keyboard: ensure all interactive elements (links, buttons, form fields, lightbox, mobile menu) are reachable via Tab, operable via Enter/Space, closeable via Escape; add visible `:focus-visible` outlines\n10. WCAG audit \u2014 color contrast: verify all text/background combinations meet 4.5:1 (normal text) and 3:1 (large text) ratios; adjust any failing combinations\n11. Responsive QA pass: test all 12 pages at 320px, 768px, 1024px, 1140px, 1440px widths; fix layout breaks, overflow issues, touch target sizes (min 44x44px)\n12. Write Vitest tests: lightbox open/close logic, animation trigger logic, verify all image assets have alt attributes in data files\n\n### Acceptance Criteria\n- [ ] Lightbox opens on image click, displays full-size image with smooth transition\n- [ ] Lightbox navigates between gallery images with prev/next, closes on X/Escape/backdrop click\n- [ ] Lightbox traps focus while open (accessibility)\n- [ ] Card hover effects are smooth (no jank), consistent across all card components\n- [ ] Menu underline animation slides smoothly on hover\n- [ ] Scroll-triggered animations fire once when elements enter viewport\n- [ ] All animations respect `prefers-reduced-motion: reduce`\n- [ ] Every `<img>` has meaningful `alt` text or `alt=\"\"` if decorative\n- [ ] All interactive elements reachable and operable via keyboard alone\n- [ ] Color contrast meets WCAG 2.1 AA on all pages (verified via axe DevTools or similar)\n- [ ] No horizontal scroll or layout overflow at any tested width\n- [ ] Lighthouse Accessibility score \u2265 95 on all pages\n\n### Dependencies\n- Phases 2-5 (all pages must exist to polish and audit)\n\n---\n\n## Phase 7: SEO Finalization, Performance Optimization & Deployment\n\n**Goal:** Complete all SEO deliverables, optimize performance for Core Web Vitals, generate deployment artifacts, and deploy to production VPS.\n\n### Deliverables\n- `public/sitemap.xml` \u2014 complete XML sitemap\n- `public/robots.txt` \u2014 search engine directives\n- All pages verified with complete meta tags, OG tags, Twitter cards, canonical URLs, Schema.org JSON-LD\n- Optimized images (Astro `<Image>` component with WebP/AVIF output, responsive `srcset`)\n- Minified CSS/JS output verified\n- Final Docker image built and tested\n- Production Nginx config with SSL (Let's Encrypt/Certbot)\n- Site deployed to VPS and live\n- Final Lighthouse audit scores\n\n### Tasks (14)\n1. Generate `public/sitemap.xml`: list all page URLs with `<lastmod>`, `<changefreq>`, `<priority>` \u2014 create build script or manual XML covering all 12+ pages\n2. Create `public/robots.txt`: allow all crawlers, reference sitemap URL, disallow `/thank-you`\n3. SEO audit pass: verify every page has unique `<title>`, `<meta name=\"description\">`, canonical `<link>`, OG tags (og:title, og:description, og:image, og:url, og:type), Twitter card tags (twitter:card, twitter:title, twitter:description, twitter:image)\n4. Schema.org audit: verify Organization JSON-LD on homepage, Product JSON-LD on Heavy 1 and GCS, WebPage on all pages, BreadcrumbList on all inner pages \u2014 validate with Google Rich Results Test\n5. Replace all `<img>` with Astro `<Image>` component where applicable for automatic WebP/AVIF generation, responsive `srcset`, and `width`/`height` attributes (CLS prevention)\n6. Audit lazy loading: ensure all below-fold images have `loading=\"lazy\"`, hero/above-fold images have `loading=\"eager\"` or `fetchpriority=\"high\"`\n7. Verify CSS/JS minification in Astro build output; ensure no unused CSS ships (audit with coverage tools)\n8. Add `<link rel=\"preload\">` for critical fonts and hero video/image; add `<link rel=\"preconnect\">` for Google Fonts, YouTube, Formspree origins\n9. Run Lighthouse CI on all pages: target Performance \u2265 90, Accessibility \u2265 95, Best Practices \u2265 95, SEO \u2265 95\n10. Finalize `Dockerfile` for production: multi-stage build (pnpm build \u2192 copy dist to Nginx Alpine), minimize image size\n11. Finalize production `nginx.conf`: SSL directives for Let's Encrypt certs, HTTP\u2192HTTPS redirect, gzip with proper MIME types, cache-control headers (1yr immutable for hashed assets, no-cache for HTML), security headers (CSP, HSTS with includeSubDomains, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy)\n12. Deploy to VPS: SSH in, install Docker + Certbot, run Certbot for SSL cert, docker compose up, verify site is live on domain with HTTPS\n13. Verify GitHub Actions deploy pipeline: push to main \u2192 build \u2192 rsync dist to VPS \u2192 Nginx picks up new files (or Docker image rebuild + restart)\n14. Post-deploy verification: test all pages live, verify forms submit, check SSL grade (SSL Labs A+), verify sitemap accessible, verify robots.txt, test OG tags with social media debuggers, check GA4 receiving data (after cookie accept)\n\n### Acceptance Criteria\n- [ ] `sitemap.xml` accessible at `/sitemap.xml`, lists all public pages with correct URLs\n- [ ] `robots.txt` accessible at `/robots.txt`, references sitemap, disallows thank-you page\n- [ ] Every page has unique, descriptive title and meta description (no duplicates)\n- [ ] Schema.org JSON-LD validates without errors for all page types\n- [ ] All images serve in WebP/AVIF format with fallbacks, responsive srcset, explicit dimensions\n- [ ] Lighthouse scores: Performance \u2265 90, Accessibility \u2265 95, Best Practices \u2265 95, SEO \u2265 95 on all pages\n- [ ] Site loads over HTTPS with valid Let's Encrypt certificate\n- [ ] SSL Labs rates site A or A+\n- [ ] Nginx returns correct security headers on all responses\n- [ ] Static assets served with long-cache headers; HTML served with no-cache\n- [ ] GitHub Actions pipeline successfully deploys on push to main\n- [ ] Contact form submits and redirects correctly in production\n- [ ] GA4 tag fires and registers pageviews in Google Analytics dashboard (after cookie consent)\n- [ ] Site renders correctly on Chrome, Firefox, Safari, Edge (latest versions) and iOS Safari / Chrome Android\n\n### Dependencies\n- All previous phases (0-6) complete\n- VPS access with SSH credentials\n- Domain DNS pointing to VPS IP\n- Formspree form ID (real or placeholder)\n\n---\n\n## Phase Summary\n\n| Phase | Name | Key Tasks | Estimated Focus Sessions |\n|-------|------|-----------|------------------------|\n| 0 | Infrastructure & Scaffold | 12 | 1 session |\n| 1 | Global Components (Header/Footer/Nav) | 11 | 1 session |\n| 2 | Home Page | 12 | 1 session |\n| 3 | Agriculture Section (5 pages) | 16 | 2 sessions |\n| 4 | Defense, Press, News, Podcast | 13 | 1-2 sessions |\n| 5 | Contact, Forms & Integrations | 10 | 1 session |\n| 6 | Lightbox, Animations, Polish & A11y | 12 | 1-2 sessions |\n| 7 | SEO, Performance & Deployment | 14 | 1-2 sessions |\n| **Total** | | **100 tasks** | **8-11 sessions** |\n\n##",
  "version": 1,
  "last_edited": "2026-03-09T03:32:12.202000"
}